import { Container } from "react-bootstrap";

const Register = () => {
    return (
        <Container>
            <h2>This is Register page</h2>
        </Container>
    );
};

export default Register;